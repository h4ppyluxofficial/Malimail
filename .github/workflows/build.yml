name: Build MaliMail
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: List Files
        run: |
          echo "Бро, смотри что я нашел в репозитории:"
          ls -R
